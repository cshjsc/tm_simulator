[package]
name = "compiler"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
combine = { version = "4.6", features = ["regex"] }
once_cell = "1.10"
regex = "1"
either = "1"